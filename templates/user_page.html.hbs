<!DOCTYPE html>
<html>

<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width" />
    <link rel="stylesheet" type="text/css" href="/public/style/style.css">
    <title>Game Book Maker</title>
</head>

<body>
    <h1>Welcome {{user}}</h1>
    <h2>Your Projects</h2>
    {{#if info.projects}}
    {{#each info.projects}}
    <p><a href="/projects/{{../user}}/{{this}}">{{this}}</a></p>
    {{/each}}
    {{else}}
    <p>User has no projects</p>
    {{/if}}
    <p><a href="/projects/{{user}}/new">Start new project</a></p>
</body>

</html>